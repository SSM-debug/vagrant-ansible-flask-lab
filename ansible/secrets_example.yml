---
# secrets_example.yml — MALL för secrets.yml
#
# Kopiera denna fil till vagrant/secrets.yml och fyll i riktiga värden.
# vagrant/secrets.yml är gitignored och får ALDRIG committas.
#
# Användning:
#   cp ansible/secrets_example.yml vagrant/secrets.yml
#   # sedan: redigera vagrant/secrets.yml med riktiga värden

# PostgreSQL — applikationsdatabas
db_name: "flaskapp"
db_user: "flaskuser"
db_password: "ÄNDRA_MIG_TILL_ETT_STARKT_LÖSENORD"

# Flask-applikationen
flask_secret_key: "ÄNDRA_MIG_TILL_EN_LÅNG_RANDOM_STRÄNG"
